version: '3'

services:
  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
    container_name: cs60_frontend
    ports:
      - "3000:3000"
    volumes:
      - ./frontend:/app
      - /app/node_modules
    environment:
      NODE_ENV: development
      NEXT_PUBLIC_API_URL: http://backend:5000  # 👉 sửa lại ở đây
    depends_on:
      - backend
    networks:
      - cs60_network

  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    container_name: cs60_backend
    ports:
      - "5000:5000"
    volumes:
      - ./backend:/app
      - /app/node_modules
    environment:
      NODE_ENV: development
    depends_on:
      - mysql
    networks:
      - cs60_network

  mysql:
    image: mysql:8.0
    container_name: cs60_mysql
    restart: always
    environment:
      MYSQL_DATABASE: cs60_recruitment
      MYSQL_USER: cs60user
      MYSQL_PASSWORD: cs60password
      MYSQL_ROOT_PASSWORD: rootpassword
    ports:
      - "3307:3306"
    volumes:
      - mysql_data:/var/lib/mysql
      - ./database/init:/docker-entrypoint-initdb.d
    networks:
      - cs60_network

  phpmyadmin:
    image: phpmyadmin/phpmyadmin
    container_name: cs60_phpmyadmin
    restart: always
    environment:
      PMA_HOST: mysql
      PMA_PORT: 3306
      PMA_USER: root
      PMA_PASSWORD: rootpassword
    ports:
      - "8080:80"
    depends_on:
      - mysql
    networks:
      - cs60_network

networks:
  cs60_network:

volumes:
  mysql_data:
